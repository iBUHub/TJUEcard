API_BASE_URL=https://your-worker.workers.dev/api
AGENT_SECRET=your-agent-secret

# Data persistence configuration (optional)
CONFIG_DIR=/config
LOG_FILE=/config/agent.log

# Optional: Pass accounts as JSON if not using config.yaml
# ACCOUNTS_JSON=[{"username": "123", "password": "abc"}]
